= form_for [current_user , @resume] do |f|
  %fieldset.form-group
    = f.text_area :document_data, :class => "tinymce form-control", :id => "resume-description", :rows => 10, :cols => 50
    = tinymce
  = render '/tags/form'
  %p
  = f.submit "Save", class: "btn btn-default"
